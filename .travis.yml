---
language: python
python:
  - "2.7"
  - "2.6"
branches:
  only:
    - master
install:
  - pip install -r requirements.txt
  - pip install coveralls
script: nosetests --with-coverage --cover-package=Playground
after_success: coveralls